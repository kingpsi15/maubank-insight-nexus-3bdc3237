
// Mock data completely removed - Enhanced MySQL database is the single source of truth
// The feedback_db MySQL database with complete schema is now used exclusively

export const mockFeedback = [];
export const mockEmployees = [];
export const mockAnalytics = {};
export const mockIssues = [];

// Note: All data now comes from the enhanced MySQL database schema
// Tables: feedback, bank_employees, issues, pending_issues, pending_resolutions, 
//         rejected_issues, feedback_resolutions, employee_feedback_interactions, feedback_issues
console.log('Mock data completely removed - using enhanced MySQL database exclusively');
